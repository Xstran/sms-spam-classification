# GloVe Embeddings

The LSTM experiment uses pretrained **100-dimensional GloVe word embeddings**.

The embedding file is intentionally not stored in this repository because of its size.

To run the LSTM + GloVe experiment, download the pretrained GloVe embeddings and place:

```text
glove.6B.100d.txt
